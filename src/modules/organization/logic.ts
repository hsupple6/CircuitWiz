import type { AnchorLogicProfile } from '../core/anchorLogic'

export const ORGANIZATION_ANCHOR_LOGIC: AnchorLogicProfile[] = [
  {
    anchorId: 'Group Box',
    domain: 'organization',
    category: 'organization',
    kicadSymbol: '(inline dashed SVG)',
    chain: 'non-electrical',
    sim: 'none',
  },
]
